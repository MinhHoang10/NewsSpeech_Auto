<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.newsspeech.auto">

    <!-- ✅ Permissions cho crawler -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".NewsApp"
        android:allowBackup="true"
        android:label="NewsSpeech Auto"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.NewsSpeechAuto"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        tools:ignore="UnusedAttribute">

        <!-- Android Auto metadata -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Android Auto Service -->
        <service
            android:name=".service.AutoSpeechService"
            android:exported="true"
            android:enabled="true"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION"/>
            </intent-filter>
        </service>

        <!-- Main Activity -->
        <activity
            android:name=".presentation.mobile.MobileActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Tắt strict mode để tránh ANR warnings -->
        <meta-data
            android:name="android.strictmode.enable"
            android:value="false" />

    </application>
</manifest>